package test.com.moseeker.rpccenter.client;

import com.moseeker.thrift.gen.profile.service.ProfileServices;
import org.apache.thrift.transport.TFastFramedTransport;
import org.apache.thrift.transport.TSocket;
import org.apache.thrift.transport.TTransport;

/**
 * Created by jack on 09/02/2017.
 */
public class ThriftClient {

    //private ProfileServices.Iface = null;

    /*private ProfileServices.Iface createIface() {
        TTransport transport = new TFastFramedTransport(new TSocket(path.getData().getIP(), path.getData().getPort(), timeout), initialBufferCapacity, maxLength);
    }*/
}
